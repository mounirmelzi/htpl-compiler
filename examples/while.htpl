<HTPL>
    let i: int;
    
    <function main(): void>    
        <while (i != 0)>
        <while (i != 0)>
            write(i);
            i = i - 1;
        </while>
        </while>
    </function>
</HTPL>
