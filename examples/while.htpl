<HTPL>
    let i: int;
    
    <function main(): void>    
        read(i);

        <if (i < 0)>
            i = -i;
        </if>

        <while (i != 0)>
            write(i);
            i = i - 1;
        </while>
    </function>
</HTPL>
