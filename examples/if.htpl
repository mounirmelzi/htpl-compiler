<HTPL>
    <function main(): void>
        let i: int;
        read(i);
        <if (i > 0)>
            i = i - 1;
        <else>
        i = i + 1;
        </if>

        write(i);
    </function>
</HTPL>
